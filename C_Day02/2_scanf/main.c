#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void) {
	int i, j;
	// float f, g;
	// int n = scanf("%d%f", &i, &f);

	// scanf("i = %d, f = %f", &i, &f); // 不要在工作时这样写！

	// scanf("%d%d%f%f", &i, &j, &f, &g);

	scanf("%d/%d", &i, &j);

	return 0;
}